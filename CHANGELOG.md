# 更新日志

本文件记录 t2048 的重要变更。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## 版本号策略

t2048 是终端应用而不是库，所以版本号描述的是**用户可见的行为契约**：

- **主版本号**：CLI 参数、按键映射、存档位置或格式发生**不兼容**变化。
- **次版本号**：向后兼容地新增按键、参数或界面元素。
- **修订号**：不改变行为的修复（渲染错误、崩溃、文案等）。

## 1.0.0 - 2026-09-21

首个完整版本。

### 新增

- 2048 全部规则：4×4 棋盘、相同数字合并、每次**有效**移动后生成一个新方块（90% 是 2，10% 是 4）、胜负判定
- vim 风格按键：`hjkl` 移动、`[count]` 重复（`3j` / `3u` / `2Ctrl-r`）、`u` / `Ctrl-r` 撤销重做、`K` 求助、`ZZ` / `ZQ` 退出
- 撤销 / 重做，可一路退到开局再走回来
- 到达 2048 后可选择继续玩下去
- 最高分持久化，下次启动自动读取；`ZQ` 退出则不写入
- 结束与帮助浮层，与棋盘同宽对齐
- 真彩色经典配色，`NO_COLOR` 或 `--mono` 时回退到 256 色
- 命令行参数：`--help`、`--mono`、`--no-color`、`--seed <N>`
- 布局随窗口自适应；窗口小于 33 × 24 时给出明确提示
- 退出与 panic 时均恢复终端状态（退出备用屏幕、恢复光标、关闭 raw 模式）
- 93 个测试，覆盖规则、按键映射与渲染

### 说明

- 已在 Linux x86_64 与 Windows x86_64 (MSVC) 上构建并通过全部测试；macOS 未实测。
- 需要 Rust 1.88 或更高（使用了 edition 2024 的 let-chains）。
- 终端至少 33 × 24 列。
- 最高分位置：Linux / macOS 为 `$XDG_DATA_HOME/t2048/best`（否则 `~/.local/share/t2048/best`），Windows 为 `%APPDATA%\t2048\best`。
